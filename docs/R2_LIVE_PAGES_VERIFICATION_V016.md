# R2 v0.16 GitHub Pages 公网验收

版本日期：2026-08-05

## 状态

R2 v0.15 的通过结论已经撤销。R2 v0.16 在 PR #12 完成本地可读性整改后，已合并到 `main`，并由新的 `Live Pages audit` 直接访问 GitHub Pages 公网页面进行验收。

产品合并提交：

```text
b4af350024470ea83a4a161feec2bb2158788ce1
```

公网证据提交：

```text
deb1a814669b5a423e040f64c1fcddeff9f0833e
```

## 公网验收结果

- HTTP：`200`
- 部署等待次数：`1`
- v0.16 的 2 个 CSS 与 4 个 JavaScript 资源：全部存在
- 浏览器错误：`0`
- 公网严格断言：`0`
- 横向溢出：`0 px`

### 公网布局

1735×865：

- 主实验台：`1575 px` 宽，左右各保留 `80 px` 安全沟槽；
- 同步分析区位于主实验台下方，不再压在右侧；
- 机制模块和判据模块各约 `778.5 px` 宽；
- 左侧按钮右边界：`76 px`，主内容左边界：`80 px`；
- 右侧按钮左边界：`1659 px`，主内容右边界：`1655 px`；
- 两侧按钮均未覆盖主内容。

### 完整呈现

- 主 Canvas 可见比例：`1.000`，底部安全距离约 `61.7 px`；
- 机制模块可见比例：`1.000`，底部安全距离约 `36.3 px`；
- 判据模块可见比例：`1.000`，底部安全距离约 `36.3 px`；
- 参数抽屉完整位于视口内。

### 真实交互

公网真实拖动物体顶部：

```text
物距：270 px → 320 px
物高：150 px → 195 px
```

### 公网 Canvas 文字

公网验收包装了真实 `canvasAPI`，记录最终画面中实际发生的全部文字绘制。结果中没有出现低于 14 px、超出安全边距或只显示部分字形的文字。

主图关键文字的实际显示字号包括：

- 区域标题：`22 px`
- 镜面端点说明：`19.8 px`
- 物体、虚像与观察者：约 `20.9 px`
- 顶部/底部必要点：`19.8 px`
- 当前判据：`27.5 px`
- 判据说明与物距/像距：约 `18.7–19.8 px`
- 底部两行物理说明：`19.8 px`

机制模块和判据模块的全部实际文字也通过最低字号和四边安全距离检查。

## 持久证据

公网验收成功后，工作流自动把原尺寸截图和指标写入：

```text
docs/live-evidence/r2-v016/
```

包含：

- `metrics.json`
- `main.png`
- `mechanism.png`
- `observable.png`
- `parameters.png`

若公网验收失败，该目录不会更新。

## 结论

R2 v0.16 已完成：

```text
用户否决 v0.15
→ 根因分析
→ 核心标准 v1.3
→ 全宽主实验台与下方分析区重构
→ 五视口有效字号和完整模块审计
→ Canvas 全量实际文字审计
→ 原尺寸逐字本地复核
→ 合并 main
→ GitHub Pages 公网资源、布局、交互、字号和完整性审计
→ 持久化公网证据
```

本轮范围仍为桌面端；手机纵向窄屏不在完成声明内。
