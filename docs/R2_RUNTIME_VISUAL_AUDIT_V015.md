# R2 平面镜虚像：真实运行与视觉验收（v0.15）

## 目标

本轮只处理 R2「平面镜虚像」。依据 v1.1 主标准和 v1.2 增补，先建立当前版本的真实浏览器基线，再根据截图与任务结果返工，不从 R1 的修复机械复制界面。

## R2 的统一底层模型

R2 的本质不是“画一个对称箭头”，而是一条完整的信息链：

```text
物体每个点发出发散光
→ 有限镜面截获其中一部分
→ 镜面按局部反射定律改变方向
→ 只有进入瞳孔的光参与当前观察
→ 视觉系统按到达方向反向追迹
→ 多条反向延长线交于镜面对称点
→ 眼睛/相机可记录虚像，但镜后屏幕接不到真实会聚光
```

因此主实验台必须让用户同时辨认：物体、有限镜面、眼睛、真实入射光、真实反射光、反向延长线、虚像和必要反射点。

## 必须完成的学习任务

1. 拖动物体，确认物距、物高与虚像同步变化，且物像关于镜面对称；
2. 拖动眼睛，确认真实反射路径与有限镜面的必要命中点变化，但虚像位置不因观察者改变；
3. 缩短镜面，使物体顶部或底部必要光线被截断；
4. 恢复足够镜高，确认完整物体重新可见；
5. 区分镜前实线、镜后虚线和镜后屏幕判据；
6. 在五个桌面视口中检查默认态、拖动态、有限镜面失败态和抽屉开启态。

## 物理验收条件

- 像距大小始终等于物距；
- 像高始终等于物高，放大率为 +1；
- 眼睛移动不改变虚像几何位置；
- 进入眼睛的每条反射光与对应反向延长线共线；
- 有限镜面完整可见判据由顶部和底部两条必要光线共同决定；
- 镜后屏幕不能在虚像点接收到真实会聚光。

## 视觉验收条件

以下任一情况均视为失败：

- 真实光线和反向延长线颜色或线型难以区分；
- 物体、虚像、眼睛或镜面被缩小到难以定位；
- 镜面有效区和非有效区无法区分；
- 必要反射点标签碰撞、贴边或被裁切；
- 主实验台大面积空白，而关键光路集中成小团；
- 宽屏仍被旧版 1540px 容器限制；
- 两个分析模块 Canvas 比例失真；
- 抽屉或边缘按钮遮挡关键光路；
- 用户无法一眼回答“光实际走到哪里”和“为什么像看起来在镜后”。

## 目标视口

- 2560 × 1440
- 1920 × 1080
- 1735 × 865
- 1440 × 900
- 1366 × 768

## 发布闭环

本轮完成声明必须经过：分支终验 → 人工逐图检查 → 合并 `main` → GitHub Pages 发布 → 无缓存线上复验。PR 未合并时只能称为开发候选版本。
